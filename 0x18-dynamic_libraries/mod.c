#include <stdio.h>

/**
 * mod - finds the remainder when one integer is divided by another.
 * @a: first int
 * @b: second int
 *
 * return: remainder of the integers
 * */
int mod(int a, int b)
{
	 return (a % b);
}
